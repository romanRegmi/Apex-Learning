/*
* Static methods are not tied to any specific instance and often provide utility or helper functions that don't rely on instance-specific data
* Static methods can only access other static members (variables or methods) of the class and cannot access instance-specific data.
* Use static methods for operations that are common across all instances or don't require access to instance-specific state.
*/

public class HelperClass {
    public static void staticMethod() {
        // This is a static method
    }
}